import React from 'react'

const PlayQuiz = ({quizes, quizTitle, quizQuestions}) =>{
  return (
    <div>
        <h1>{quizes.title}</h1>
        <div>

        </div>
    </div>
  )
}

export default PlayQuiz